@model Summa.Forms.Models.Question

<div>@Model.Value</div>

@{
    foreach (var option in Model.Options.OrderBy(x => x.Index))
    {
        <input type="radio" id="@option.Id" name="group" value="@option.Value">
        <label for="@option.Id">@option.Value</label><br>
    }
}